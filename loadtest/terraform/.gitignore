# Terraform state / 시크릿 — 커밋 금지
*.tfstate
*.tfstate.*
.terraform/
terraform.tfvars
*.auto.tfvars
*.tfplan
.terraform.lock.hcl
crash.log
tfplan

# load-test 전용 secret env — 커밋 금지 (example 은 커밋)
load-test.env
!load-test.env.example
