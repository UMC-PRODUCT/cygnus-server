package com.umc.product.form.domain;

/** Form ownership policy가 평가하는 operation 종류. */
public enum FormOperation {
    MANAGE_STRUCTURE,
    PUBLISH,
    DELETE,
    READ,
    RESPOND,
    READ_RESPONSES
}
